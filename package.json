{
  "name": "moona-studio-site",
  "private": true,
  "description": "Static site. The only dependency belongs to the /api lead handler.",
  "scripts": {
    "test:e2e": "npm --prefix tests test",
    "vercel-build": "node scripts/ensure-posthog.js",
    "check:posthog": "node scripts/ensure-posthog.js --check"
  },
  "dependencies": {
    "nodemailer": "^9.0.6"
  }
}
